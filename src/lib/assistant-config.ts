export let assistantId = "";

// If no assistant ID is set, we'll create a new assistant during initialization
if (assistantId === "") {
  // We'll set this dynamically when creating a new assistant
}
